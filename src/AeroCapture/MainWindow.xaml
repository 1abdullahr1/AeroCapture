<Window
    x:Class="AeroCapture.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AeroCapture">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!-- Main Navigation View -->
        <NavigationView x:Name="MainNavView" 
                        PaneDisplayMode="LeftCompact" 
                        IsBackButtonVisible="Collapsed" 
                        IsSettingsVisible="True"
                        SelectionChanged="OnNavSelectionChanged"
                        Loaded="OnNavLoaded">
            
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Dashboard" Tag="Dashboard" Icon="Home" IsSelected="True" />
                <NavigationViewItem Content="History" Tag="History" Icon="Folder" />
            </NavigationView.MenuItems>

            <NavigationView.PaneFooter>
                <StackPanel Margin="10,0,10,12" Spacing="4">
                    <TextBlock Text="v1.0.0" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </NavigationView.PaneFooter>

            <!-- Navigation Frame Content -->
            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
